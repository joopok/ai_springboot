# Git 자동 줄바꿈 설정
* text=auto

# 로그 파일은 LF로 강제
*.log text eol=lf

# 실행 파일은 바이너리로 처리
*.jar binary
*.war binary
*.zip binary
*.tar binary
*.gz binary

# 스크립트 파일은 LF로 강제
*.sh text eol=lf
gradlew text eol=lf

# Windows 스크립트는 CRLF로 강제
*.bat text eol=crlf
*.cmd text eol=crlf

# Java 소스 파일
*.java text eol=lf
*.properties text eol=lf
*.xml text eol=lf
*.yml text eol=lf
*.yaml text eol=lf 