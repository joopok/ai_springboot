# Server Configuration
server.port=8080

# Application Name
spring.application.name=JobKoreaBillboard

# Database Configuration
spring.datasource.url=jdbc:mariadb://192.168.0.109:3306/jobtracker?useUnicode=true&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=~Asy10131227
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# MyBatis Configuration
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.example.pm7.model
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.jdbc-type-for-null=NULL

# Logging
logging.level.root=INFO
logging.level.com.example.pm7=DEBUG
logging.level.org.mybatis=DEBUG
logging.level.java.sql=DEBUG

# JSON Configuration
spring.jackson.default-property-inclusion=NON_NULL

# CORS Configuration (handled in controller)
# Additional CORS settings can be added here if needed