# Server Configuration
server.port=8080

# Application Name
spring.application.name=JobKoreaBillboard

# Database Configuration
spring.datasource.url=jdbc:mariadb://192.168.0.109:3306/jobtracker?useUnicode=true&characterEncoding=utf8&autoReconnect=true&maxReconnects=3
spring.datasource.username=root
spring.datasource.password=~Asy10131227
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# HikariCP Connection Pool Configuration
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.auto-commit=true

# MyBatis Configuration
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.fid.job.model
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.jdbc-type-for-null=NULL

# Logging
logging.level.root=INFO
logging.level.com.fid.job=DEBUG
logging.level.org.mybatis=DEBUG
logging.level.java.sql=DEBUG

# JSON Configuration
spring.jackson.default-property-inclusion=NON_NULL

# CORS Configuration (handled in controller)
# Additional CORS settings can be added here if needed

# Socket.io Configuration
socket.io.host=0.0.0.0
socket.io.port=9092
socket.io.worker-threads=100
socket.io.allow-custom-requests=true
socket.io.upgrade-timeout=10000
socket.io.ping-timeout=60000
socket.io.ping-interval=25000